# Chat Handoff — Homeless Twenty 1904

## Current state
Static marketing site complete and ready for local preview / static deploy.

## Next agent / human tasks
1. Replace Formspree `YOUR_FORM_ID` in `index.html`.
2. Update domain in canonical, OG tags, `robots.txt`, `sitemap.xml`.
3. Swap phone, email, social URLs for real lodge contacts.
4. Replace Unsplash plaque/hero images with authentic photography.
5. Set event `payUrl` + `payReady: true` when Stripe (or bank) links exist.
6. Configure host 404 → `404.html`.

## Do not
- Do not rebuild as a SPA framework unless content/CMS needs grow substantially.
- Do not dilute brand crimson/gold/parchment for generic corporate UI.
